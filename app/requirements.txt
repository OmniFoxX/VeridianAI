# OracleAI - Python dependencies
#
# The curated, version-ranged dependency manifest lives in:
#     backend/requirements.txt
# Installing this file just forwards to it (so `pip install -r requirements.txt`
# from the project root still works).
#
# This root file used to be a raw `pip freeze` snapshot that pinned
# machine-specific CUDA builds such as torch==2.12.1+cu132 / torchvision+cu132,
# which do NOT exist on PyPI and broke fresh installs on other machines.
#
# Optional extras (NOT required to run OracleAI core):
#   - Whisper voice STT:  pip install openai-whisper sounddevice   (pulls CPU torch)
#   - NVIDIA GPU torch:   install from https://pytorch.org for your CUDA version
#   - Browser automation: pip install playwright  &&  python -m playwright install chromium
#   - Discord / BitChat:  pip install discord.py bleak

-r backend/requirements.txt
