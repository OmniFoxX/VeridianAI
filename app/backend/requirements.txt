fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-multipart>=0.0.9
requests>=2.31.0
# v2.11.15: Socials channels work out of the box. These were "optional
# extras", which shipped a Socials panel where 3 of 4 channels could never
# connect on a fresh install (Mastodon/Bluesky need aiohttp, Discord needs
# discord.py). Both are small, pure-Python-wheel installs.
aiohttp>=3.9.0
discord.py>=2.3.0
# File processing (optional but recommended)
pypdf>=4.0.0
python-docx>=1.0.0
pandas>=2.1.0
openpyxl>=3.1.0
# For direct GGUF loading (optional — needed only if backend=llamacpp):
# llama-cpp-python>=0.2.56
# Process lifecycle management (Phase 1D: tier restart endpoints)
psutil>=5.9.0
# Fernet encryption support -- imported by memory_logger_surprise.py and
# sage_daemon.py in preparation for the Phase 3 encryption work. The`from
# cryptography.fernet import Fernet` statement runs at module-load
# time, so the package must be present for the backend to boot.
cryptography>=3.0
# Inbound vision: HEIC/HEIF/AVIF decode + transcode to JPEG for the model
# servers (Ollama/llama.cpp accept JPEG/PNG, not HEIC). pillow-heif bundles
# libheif in its wheels (Windows included), so usually no separate system install.
pillow-heif>=0.13.0
Pillow>=9.0.0
# FIDO2 / passkeys (v2.13.5 auth policy): Yubico's own python-fido2, used by
# backend/mfa.py to talk to security keys DIRECTLY (WindowsClient/CTAP2-HID),
# bypassing Electron's file:// secure-context limitation. Pure-Python wheel.
# The backend boots fine without it -- FIDO2 endpoints just report "not
# installed" -- but it ships by default so YubiKey enrollment works
# out of the box.
fido2>=1.1.3
# Browser automation. NOT optional despite the note in the root requirements:
# browser_tool.py imports playwright.async_api at module level, so without it
# the [BROWSE:] tool cannot load at all -- which is what "plugin needed" meant
# on a machine that had everything else.
#
# PINNED to a minor range on purpose. The Python package and the browser build
# it downloads are versioned together; a package newer than the bundled browser
# refuses to launch it and reports the browser as "not installed", which sends
# you looking in exactly the wrong place. tools/bundle_playwright.ps1 fetches
# the matching browser.
playwright>=1.47,<1.60
# browser_tool.py imports playwright_stealth at module level too. Omitting it
# is what produced "No module named 'playwright_stealth'" in the 2.14.0 MSIX
# while the portable build worked -- the dev machine had it, the bundle did not.
playwright-stealth>=1.1.0
# Static analysis for the IDE panel's Check button and the [IDE_CHECK] tag.
# It reads code and NEVER runs it -- ast.parse builds a tree, pyflakes walks
# it, nothing is imported or evaluated -- which is why checking needs none of
# the gates running does and works in every mode with code execution off.
#
# PINNED EXACTLY, unlike most of this file, because it is the only third-party
# package here that exists to inspect the user's own code:
#   MIT licence, ZERO transitive dependencies, pure Python (no .pyd/.dll/.so),
#   62 KB wheel, sha256
#   84b5be138a2dfbb40689ca07e2152deb896a65c3a3e24c251c5c62489568074a
# Installed into every bundled runtime from that one local wheel rather than
# fetched per-tree, so all four trees carry identical bytes and a machine with
# no internet can still build. Offline boot is a hard requirement.
#
# OPTIONAL AT RUNTIME. sage_engine.check_python_code degrades to the ast-only
# syntax check when it is absent, and SAYS which checker ran, so a thin answer
# is never mistaken for a clean bill of health.
pyflakes==3.2.0
